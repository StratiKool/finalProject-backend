spring:
  datasource:
    url: jdbc:postgresql://localhost:6004/spring-2024
    username: postgres
    password: password


  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always